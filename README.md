# Serverless-Document-Processing-Pipeline-AWS-
Serverless AWS pipeline that ingests PDFs from S3, extracts text using Textract, stores results in DynamoDB, and exposes secure REST APIs via API Gateway with CloudWatch logging.
